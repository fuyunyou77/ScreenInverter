<Window x:Class="ScreenInverter.InverterOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Screen Inverter"
        Width="400"
        Height="300"
        MinWidth="100"
        MinHeight="100"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        ResizeMode="NoResize"
        UseLayoutRounding="True">

    <Window.Resources>
        <Style x:Key="DarkComboBoxStyle"
                TargetType="ComboBox">
            <Setter Property="Background"
                    Value="#FF333333"/>
            <Setter Property="Foreground"
                    Value="White"/>
            <Setter Property="BorderBrush"
                    Value="#FF555555"/>
            <Setter Property="BorderThickness"
                    Value="1"/>
            <Setter Property="Padding"
                    Value="5,0"/>
            <Setter Property="VerticalContentAlignment"
                    Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="MainBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="2"/>

                            <Grid Margin="{TemplateBinding Padding}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <ContentPresenter Grid.Column="0"
                                                  IsHitTestVisible="False"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"/>

                                <Path Grid.Column="1"
                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                      Fill="White"
                                      VerticalAlignment="Center"
                                      Margin="5,0,2,0"/>
                            </Grid>

                            <ToggleButton Background="Transparent"
                                    BorderThickness="0"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>

                            <Popup x:Name="PART_Popup"
                                    AllowsTransparency="True"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Bottom">
                                <Border Background="#FF333333"
                                        BorderBrush="#FF555555"
                                        BorderThickness="1"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer MaxHeight="300">
                                        <ItemsPresenter SnapsToDevicePixels="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background"
                    Value="#FF333333"/>
            <Setter Property="Foreground"
                    Value="White"/>
            <Setter Property="Padding"
                    Value="5,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted"
                                    Value="True">
                                <Setter TargetName="ItemBorder"
                                        Property="Background"
                                        Value="#FF555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="MainBorder"
            BorderBrush="#40FFFFFF"
            BorderThickness="2"
            Background="Transparent"
            SnapsToDevicePixels="True">
        <Grid x:Name="MainGrid">
            <Image x:Name="CapturedImage"
                   Stretch="Fill"
                   IsHitTestVisible="False"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   SnapsToDevicePixels="True"/>

            <TextBlock x:Name="StatusText"
                       Text="已锁定"
                       Foreground="#00FF00"
                       FontSize="24"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Visibility="Collapsed"
                       IsHitTestVisible="False">
                <TextBlock.Effect>
                    <DropShadowEffect Color="Black"
                            BlurRadius="4"
                            ShadowDepth="0"/>
                </TextBlock.Effect>
            </TextBlock>

            <Border x:Name="ControlBar"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Stretch"
                    Background="#CC000000"
                    Height="30"
                    Visibility="Visible">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               x:Name="ShortcutHintText"
                               Text="快捷键 锁定/解锁 (穿透模式)"
                               VerticalAlignment="Center"
                               Foreground="#E0E0E0"
                               Margin="10,0,0,0"
                               FontSize="12"/>

                    <ComboBox Grid.Column="1"
                              x:Name="CmbDpiScale"
                              Style="{StaticResource DarkComboBoxStyle}"
                              Width="85"
                              SelectedIndex="0"
                              SelectionChanged="CmbDpiScale_SelectionChanged"
                              Margin="5,2,5,2"
                              FontSize="11">
                        <ComboBoxItem Content="DPI: 自动"
                                Tag="Auto"/>
                        <ComboBoxItem Content="DPI: 100%"
                                Tag="100"/>
                        <ComboBoxItem Content="DPI: 125%"
                                Tag="125"/>
                        <ComboBoxItem Content="DPI: 150%"
                                Tag="150"/>
                        <ComboBoxItem Content="DPI: 200%"
                                Tag="200"/>
                    </ComboBox>

                    <Button Grid.Column="2"
                            x:Name="ModeButton"
                            Content="模式：智能文档"
                            Click="ModeButton_Click"
                            Foreground="#FFFFFF"
                            Background="#CC333333"
                            BorderBrush="#666666"
                            BorderThickness="1"
                            Margin="5,2,5,2"
                            FontSize="11"/>

                    <Button Grid.Column="3"
                            Content="✕"
                            Click="CloseButton_Click"
                            Foreground="White"
                            Background="#C03030"
                            BorderThickness="0"
                            Margin="5,2,5,2"
                            Width="24"
                            FontSize="14"/>
                </Grid>
            </Border>

            <Thumb Width="50"
                    Height="50"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                   Margin="-15,-15,0,0"
                    Cursor="SizeNWSE"
                   DragStarted="OnResizeDragStarted"
                    DragDelta="OnTopLeftCornerResize"
                    DragCompleted="OnResizeDragCompleted">
                <Thumb.Template>
                    <ControlTemplate>
                        <Rectangle Fill="Transparent"/>
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>
            <Thumb Width="50"
                    Height="50"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Bottom"
                   Margin="-15,0,0,-15"
                    Cursor="SizeNESW"
                   DragStarted="OnResizeDragStarted"
                    DragDelta="OnBottomLeftCornerResize"
                    DragCompleted="OnResizeDragCompleted">
                <Thumb.Template>
                    <ControlTemplate>
                        <Rectangle Fill="Transparent"/>
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>
            <Thumb Width="50"
                    Height="50"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                   Margin="0,0,-15,-15"
                    Cursor="SizeNWSE"
                   DragStarted="OnResizeDragStarted"
                    DragDelta="OnBottomRightCornerResize"
                    DragCompleted="OnResizeDragCompleted">
                <Thumb.Template>
                    <ControlTemplate>
                        <Rectangle Fill="Transparent"/>
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>
            <Thumb Height="15"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Top"
                   Margin="0,-15,-150,0"
                    Cursor="SizeNS"
                   DragStarted="OnResizeDragStarted"
                    DragDelta="OnTopEdgeResize"
                    DragCompleted="OnResizeDragCompleted">
                <Thumb.Template>
                    <ControlTemplate>
                        <Rectangle Fill="Transparent"/>
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>
            <Thumb Height="15"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Bottom"
                   Margin="0,0,0,-15"
                    Cursor="SizeNS"
                   DragStarted="OnResizeDragStarted"
                    DragDelta="OnBottomEdgeResize"
                    DragCompleted="OnResizeDragCompleted">
                <Thumb.Template>
                    <ControlTemplate>
                        <Rectangle Fill="Transparent"/>
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>
            <Thumb Width="15"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Stretch"
                   Margin="-15,0,0,0"
                    Cursor="SizeWE"
                   DragStarted="OnResizeDragStarted"
                    DragDelta="OnLeftEdgeResize"
                    DragCompleted="OnResizeDragCompleted">
                <Thumb.Template>
                    <ControlTemplate>
                        <Rectangle Fill="Transparent"/>
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>
            <Thumb Width="15"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Stretch"
                   Margin="0,30,0,-15"
                    Cursor="SizeWE"
                   DragStarted="OnResizeDragStarted"
                    DragDelta="OnRightEdgeResize"
                    DragCompleted="OnResizeDragCompleted">
                <Thumb.Template>
                    <ControlTemplate>
                        <Rectangle Fill="Transparent"/>
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>

            <!-- 独立的锁定解锁按钮，浮于最高层 -->
            <Button x:Name="LockButton"
                    Width="40" Height="40"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="0,40,10,0"
                    Background="#80000000" Foreground="White"
                    BorderThickness="1" BorderBrush="#80FFFFFF"
                    FontSize="20"
                    Click="LockButton_Click"
                    ToolTip="点击锁定">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="20"/>
                    </Style>
                </Button.Resources>
                <TextBlock x:Name="LockIcon" Text="🔓" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,-2,0,0"/>
            </Button>
        </Grid>
    </Border>
</Window>