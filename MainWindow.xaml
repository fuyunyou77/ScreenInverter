<Window x:Class="ScreenInverter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="屏幕颜色反转器 - 设置"
        Height="480" Width="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        Closing="Window_Closing">

    <Window.Resources>
        <!-- 全局文本颜色 -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
        </Style>

        <!-- 暗色 ComboBox 样式 -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3"/>
                            <Grid Margin="{TemplateBinding Padding}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0"
                                                  IsHitTestVisible="False"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"/>
                                <Path Grid.Column="1"
                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                      Fill="#AAAAAA"
                                      VerticalAlignment="Center"
                                      Margin="5,0,2,0"/>
                            </Grid>
                            <ToggleButton Background="Transparent"
                                          BorderThickness="0"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Popup AllowsTransparency="True"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Bottom">
                                <Border Background="#2D2D2D"
                                        BorderBrush="#555555"
                                        BorderThickness="1"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer MaxHeight="300">
                                        <ItemsPresenter SnapsToDevicePixels="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#3E3E3E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 暗色 TextBox 样式 -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CaretBrush" Value="#E0E0E0"/>
            <Setter Property="Padding" Value="4,2"/>
        </Style>

        <!-- 暗色 Button 样式 -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3C3C3C"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#4A4A4A"/>
                                <Setter TargetName="BtnBorder" Property="BorderBrush" Value="#6A6A6A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#2A2A2A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="控制面板"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="#FFFFFF"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,15"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                <TextBlock Text="穿透快捷键：" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="CmbModifier" Width="70" SelectedValuePath="Value">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Key}" Foreground="#E0E0E0"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <TextBlock Text=" + " VerticalAlignment="Center" Margin="5,0"/>
                <ComboBox x:Name="CmbActionKey" Width="70" SelectedValuePath="Value">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Key}" Foreground="#E0E0E0"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>

            <Border Background="#2A2520" BorderBrush="#665820" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,15">
                <TextBlock Text="💡 提示：WPF 坐标系只与 DPI 缩放相关。&#10;请选择「系统缩放比例」，分辨率设置通常无需操作。"
                           FontSize="12"
                           Foreground="#D4A84A"
                           TextWrapping="Wrap"/>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                <TextBlock Text="显示器分辨率：" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="CmbResolution" Width="180" SelectionChanged="CmbResolution_SelectionChanged" IsEnabled="False">
                    <ComboBoxItem Content="自动获取 (推荐)" Tag="Auto"/>
                    <ComboBoxItem Content="1080p (1920x1080)" Tag="1080p"/>
                    <ComboBoxItem Content="2K (2560x1440)" Tag="2K"/>
                    <ComboBoxItem Content="4K (3840x2160)" Tag="4K"/>
                    <ComboBoxItem Content="自定义分辨率..." Tag="Custom"/>
                </ComboBox>
            </StackPanel>

            <StackPanel x:Name="PanelCustomRes" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10" Visibility="Collapsed">
                <TextBox x:Name="TxtResW" Width="60" TextAlignment="Center" VerticalContentAlignment="Center"/>
                <TextBlock Text=" × " VerticalAlignment="Center" Margin="5,0"/>
                <TextBox x:Name="TxtResH" Width="60" TextAlignment="Center" VerticalContentAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,5">
                <TextBlock Text="系统缩放比例：" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="CmbDpiScale" Width="180" SelectionChanged="CmbDpiScale_SelectionChanged">
                    <ComboBoxItem Content="自动获取 (推荐)" Tag="Auto"/>
                    <ComboBoxItem Content="100%" Tag="100"/>
                    <ComboBoxItem Content="125%" Tag="125"/>
                    <ComboBoxItem Content="150%" Tag="150"/>
                    <ComboBoxItem Content="175%" Tag="175"/>
                    <ComboBoxItem Content="200%" Tag="200"/>
                    <ComboBoxItem Content="自定义比例..." Tag="Custom"/>
                </ComboBox>
            </StackPanel>

            <StackPanel x:Name="PanelCustomDpi" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15" Visibility="Collapsed">
                <TextBox x:Name="TxtDpiScale" Width="60" TextAlignment="Center" VerticalContentAlignment="Center"/>
                <TextBlock Text=" %" VerticalAlignment="Center" Margin="5,0"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,15">
                <TextBlock Text="关闭行为：" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="CmbCloseBehavior" Width="200">
                    <ComboBoxItem Content="最小化到系统托盘" Tag="MinimizeToTray"/>
                    <ComboBoxItem Content="直接退出程序" Tag="Exit"/>
                </ComboBox>
            </StackPanel>

            <Button Content="保存并应用所有设置"
                    Click="SaveButton_Click"
                    Width="150"
                    Margin="0,10,0,15"/>

            <Button Content="开启 / 关闭反转遮罩"
                    Click="ToggleOverlayButton_Click"
                    Padding="20,10"
                    FontSize="14"
                    Background="#1A6FB5"
                    Foreground="White"
                    BorderBrush="#2080D0"/>

            <TextBlock Text="提示：在强烈的黑白交界处会出现白色光晕并非bug"
                       HorizontalAlignment="Center"
                       Margin="0,15,0,0"
                       Foreground="#777777"
                       FontSize="11"/>
        </StackPanel>
    </Grid>
</Window>
